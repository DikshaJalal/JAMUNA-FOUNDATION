// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows: expect(element).toHaveTextContent(/react/i)
// keep default empty or add testing setup if needed
import '@testing-library/jest-dom';
